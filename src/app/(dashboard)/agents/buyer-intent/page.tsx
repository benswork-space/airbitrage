import { AgentTabView } from '@/components/agents/agent-tab-view';

export default function BuyerIntentPage() {
  return <AgentTabView agentType="buyer-intent" agent={undefined} opportunities={[]} runs={[]} />;
}
